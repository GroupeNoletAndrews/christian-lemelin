# Système de design — Entreprises Christian Lemelin

> **Source de vérité du design.** À consulter **avant de créer ou modifier toute UI**, et à **mettre à jour à chaque changement de design** (voir [Journal](#journal-des-modifications) en bas).
>
> Référence visuelle : template Framer **OPUS** — https://opus-template.framer.website/

---

## 1. Principes

Le langage visuel est **simple, épuré et aéré**, calqué sur OPUS :

- **Espace avant tout** — du whitespace généreux entre sections et autour du contenu. Laisser respirer.
- **Retenue** — peu d'éléments par écran, une hiérarchie claire, pas de décor superflu.
- **Un seul accent fort** — le bleu électrique, utilisé avec parcimonie (liens, CTA, détails). Le reste est neutre.
- **Hairlines, pas de boîtes lourdes** — séparateurs fins (`border-border`) plutôt que cartes contrastées.
- **Mouvement sobre** — animations discrètes, jamais gratuites ; toujours respecter `prefers-reduced-motion`.

---

## 2. Couleurs

Tokens définis dans [`src/app/globals.css`](src/app/globals.css) (`@theme inline`). **Toujours utiliser les classes Tailwind ci-dessous — jamais de hex en dur dans les composants.**

| Token | Valeur | Usage | Classe Tailwind |
|---|---|---|---|
| `background` | `#f3f3f1` | Fond de page (crème) | `bg-background` |
| `surface` / `surface-elevated` | `#ffffff` | Cartes, panneaux élevés | `bg-surface` |
| `foreground` | `#141414` | Texte principal (noir **neutre**, pas de vert) | `text-foreground` |
| `foreground-muted` | `rgba(20,20,20,.6)` | Texte secondaire / légendes | `text-foreground-muted` |
| `ink` | `#111111` | Fond des sections sombres (neutre) | `bg-ink` (texte `text-white`) |
| `accent` | `#0048f9` | Accent bleu — **uniquement** flèche de lien et fond de bouton | `bg-accent` |
| `accent-hover` | `#0039c4` | Survol de l'accent (dérivé) | `bg-accent-hover` |
| `border` | `rgba(151,151,151,.2)` | Séparateurs fins (hairlines) | `border-border` |

⚠️ **Pas de texte bleu.** Le bleu ne sert que pour : la flèche `↗` des liens et le fond des boutons (texte blanc dessus). Jamais pour du texte (titres, chiffres, codes…). Pas de teinte verte nulle part — les noirs sont neutres.

> 🏠 **Page d'accueil — aucun bleu (demande client).** Sur `/` **uniquement**, l'accent bleu est neutralisé en **noir** ; les pages internes gardent le bleu. Mécanisme dans [`globals.css`](src/app/globals.css) : `--color-accent`/`--color-accent-hover` sont sortis du bloc `@theme inline` vers un `@theme` **runtime** (les utilitaires compilent donc en `var(--color-accent)`), puis réassignés via **`body:has([data-home-neutral])`** → la valeur cascade dans le header, le footer et toutes les sections **sans toucher chaque composant**. Le marqueur `[data-home-neutral]` est rendu par [`SiteChrome`](src/components/layout/SiteChrome.tsx) seulement sur `/`. Les **sections sombres** (`data-header-theme="dark"`) repassent l'accent en **blanc** (sinon un accent noir disparaîtrait sur fond noir) ; un `bg-accent` devenu blanc y garde un **label foncé** (bouton « Nous joindre »). Focus-visible et `::selection` aussi neutralisés sur `/`.

> ⚠️ **Migration en cours :** certains composants utilisent encore `text-zinc-*`, `black/8`, `black/12`. À remplacer progressivement par les tokens ci-dessus.

---

## 3. Typographie

Chargées via `next/font/google` dans [`src/app/layout.tsx`](src/app/layout.tsx).

| Police | Rôle | Classe |
|---|---|---|
| **Onest** (variable) | Titres **et** corps de texte | `font-sans` / `font-display` (par défaut sur `body`) |
| **Fragment Mono** (400) | Labels, codes | `font-mono` |

**Conventions :**
- **Titres** : Onest, **casse normale** (pas de capitales), poids medium/semibold, grande taille, `leading` serré (`leading-[1.05]`). OPUS n'utilise pas de capitales condensées.
- **Labels** : Fragment Mono, petit, `uppercase`, `tracking` large (`tracking-[0.18em]`), en `text-foreground-muted`.
- **Corps** : Onest, poids normal, `leading-relaxed`, `text-foreground-muted` pour le secondaire.
- Tailles fluides via `clamp()` pour les grands titres.

> ⚠️ **Migration en cours :** les composants hérités utilisent `font-display` en `uppercase` (esthétique Bebas Neue). À convertir vers des titres Onest en casse normale lors des prochaines passes.

---

## 4. Espacement & layout

- **Padding de section** généreux : viser `py-24`/`py-32` desktop, `py-16`/`py-20` mobile.
- **Largeur de contenu** bornée (`max-w-*`) avec marges latérales confortables (`px-6` mobile, `px-14`+ desktop).
- Espacements verticaux à l'échelle Tailwind ; privilégier la respiration au remplissage.
- Grilles simples, alignées ; éviter la surcharge d'éléments.

---

## 5. Composants

**Primitives réutilisables** (dans [`src/components/ui/`](src/components/ui/)) — **à réutiliser, ne pas réinventer** :
- [`ArrowLink`](src/components/ui/ArrowLink.tsx) — lien bleu avec flèche `↗`.
- [`Marquee`](src/components/ui/marquee.tsx) — ruban défilant en boucle (pattern ui-layouts/magicui). Props `reverse`/`pauseOnHover`/`vertical`/`repeat` ; vitesse et espacement via les custom properties `--duration`/`--gap` (réglables par breakpoint). L'animation (`@keyframes marquee` + `--animate-marquee`) vit dans [`globals.css`](src/app/globals.css) et est **désactivée sous `prefers-reduced-motion`**. Helper `cn()` dans [`src/lib/utils.ts`](src/lib/utils.ts).
- [`linear-modal`](src/components/ui/linear-modal.tsx) — **dialogue morphant** (`Dialog`/`DialogTrigger`/`DialogContainer`/`DialogContent`/`DialogImage`/`DialogTitle`/`DialogDescription`/`DialogClose`, pattern ui-layouts/motion-primitives) : une carte-trigger se **déplie** (shared `layoutId` sur le conteneur, l'image et le titre) en un panneau **portalisé** (`createPortal`), avec **scroll-lock** (Lenis + `body`), **Escape**/clic-extérieur, gestion du focus. Fallback `prefers-reduced-motion` = simple fondu (pas de morph). Prop **`morph`** (défaut `true`) : `morph={false}` désactive le shared-element (le modal **fond/zoome** au lieu de redimensionner l'image du trigger — évite les déplacements parasites). Le voile sombre intégré (`bg-black/92` + `backdrop-blur`) est activable via la prop **`dim`** (défaut `true`) ; `dim={false}` seulement quand le contenu fournit son propre fond. Le modal matériaux utilise désormais ce voile par défaut (plus de shader derrière le modal). **`DialogContent` accepte un `style` de type `MotionStyle`** (pas seulement `CSSProperties`) → on peut lui passer des `MotionValue` de transform (ex. le `rotateX`/`rotateY` du tilt 3D du modal matériaux).
- [`ShaderBackdrop`](src/components/ui/ShaderBackdrop.tsx) — gradient animé **monochrome** (`@shadergradient/react`, WebGL) chargé en `dynamic`/`ssr:false`, derrière une **error boundary** qui retombe sur un dégradé CSS statique si le runtime WebGL échoue. Tons noir/gris/blanc **uniquement**. *(Actuellement non utilisé — disponible comme primitive.)*
- [`RandomizedTextEffect`](src/components/ui/text-randomized.tsx) — **scramble-reveal** (pattern ui-layouts « text-randomized ») re-thématisé ECL : le texte **se résout de gauche à droite** pendant que les slots non résolus **clignotent à travers les symboles d'éléments métalliques** du tableau périodique (Fe, Al, Cu, Ti…). Pool aplati en **glyphes simples** (pas de reflow horizontal du gros titre). Prop **`start`** (déclenche l'animation), `speed`/`revealRate` (cadence). Tout en timer (jamais de `setState` synchrone dans l'effet → pas de mismatch d'hydratation) ; **`prefers-reduced-motion`** = texte final immédiat. Vrai texte en `sr-only` (a11y/SEO), le clignotement est `aria-hidden`. Utilisé par le **titre du `Hero`**.
- [`ContactFab`](src/components/ui/ContactFab.tsx) — **bouton « Contactez-nous »** flottant en bas à droite : pill **noir (`bg-foreground`), icône + label, non masquable** (pas de croix). Au **clic**, il déplie un panneau noir avec le message complet (« Un projet métal en tête ? » + délai 24 h) et un lien `/contact` ; clic-extérieur / Échap referment le panneau. Affiché sur **toutes les pages internes** (jamais sur `/` ni `/contact`) ; l'accueil garde la grande section `ContactCTA`. Apparaît après ~1 s (entrée spring, fallback reduced-motion). Rendu par [`SiteChrome`](src/components/layout/SiteChrome.tsx).

> **Pas de pilules/badges.** Ni eyebrow (pill point bleu + label en tête de section), ni tag (pill outline de catégorie/matériau). Les sections s'ouvrent directement sur leur titre ; les métadonnées s'écrivent en texte simple.

- **Boutons / CTA** : pill `rounded-full`, fond `bg-accent`, **texte blanc** (jamais texte sombre sur le bleu), survol `bg-accent-hover`. Variante secondaire : bordure `border-border` + texte `text-foreground`.
- **Cartes / panneaux** : `bg-surface`, séparation par `border-border` (hairline) plutôt qu'ombres marquées.
- **Focus** : contour bleu `#0048f9` (défini globalement via `:focus-visible`). Ne pas le retirer.

---

## 6. Motion

- Smooth-scroll déjà en place (Lenis / GSAP ScrollTrigger).
- Animations d'entrée discrètes (fondu, léger translate).
- **Toujours** garder un fallback `prefers-reduced-motion` (déjà respecté dans `Materiaux.tsx`).

---

## 7. Layout & patterns OPUS *(observés sur le template — à reproduire)*

Captures de référence prises au 1440px. Le layout d'OPUS repose sur ces patterns — **toute nouvelle section doit s'en inspirer.**

- **Navigation** : barre **minimale** transparente (logo à gauche + un seul bouton pill « Menu » bleu à droite), repliée sur **tous** les écrans comme OPUS. Au clic → **overlay plein écran noir** (`bg-black`) : liens en très grand (Onest) à gauche, contact + bouton « Nous joindre » blanc à droite. La **barre flotte au-dessus de l'overlay** (`z-[96]`, sous le preloader/modal `z-100`) et le **même bouton** bascule **en place** en « Fermer » (pill blanc, texte foncé) : c'est l'**unique fermeture, pas de ✕ séparé** — donc toujours parfaitement aligné avec « Menu » (plus de décalage). L'overlay n'a plus sa propre rangée logo+✕ (le logo de la barre flottante, inversé blanc, sert aux deux états) ; le **cadre du menu est descendu sous la barre** (`pt-24` sur le wrapper) pour qu'aucune ligne (bordure du cadre / divider) ne chevauche le logo, sur mobile comme desktop. Sticky : fond `backdrop-blur` + hairline au scroll. **Auto-hide directionnel** : la barre **se cache (slide `translateY(-100%)`) au scroll vers le bas** (après ~120 px) et **réapparaît animée au scroll vers le haut** (`motion.header`, dead-zone de 6 px anti-jitter ; reste visible si le menu est ouvert).
  - **Animation d'ouverture/fermeture (façon pointlaz.com)** : révélation **graduelle ligne par ligne** — chaque lien **monte depuis un masque** (`overflow-hidden`) et un **trait se dessine** (`scaleX`) sous lui, en cascade (`custom={i}`, ~1,7 s). La **fermeture** rejoue l'inverse (liens redescendent dans le masque, traits se rétractent) puis l'overlay s'efface. Variants `overlayV`/`linkV`/`lineV`/`fadeV` dans [`Header.tsx`](src/components/layout/Header.tsx).
- **Liens** : texte **souligné** (couleur neutre `text-foreground` ou `text-white`), souligné fin `underline-offset`, + flèche **`↗` bleue**. Le texte n'est **jamais** bleu. Composant [`ArrowLink`](src/components/ui/ArrowLink.tsx).
- **Parallax images** : sur les cartes (Solutions, Réalisations, Matériaux), l'image est sur-dimensionnée (**170%**) et translate verticalement (`amount` ~19%) au scroll → effet marqué « les images me suivent ». Composant [`ParallaxImage`](src/components/ui/ParallaxImage.tsx), respecte `prefers-reduced-motion`, fluide via Lenis.
- **Hero** : **bannière vidéo plein cadre** (`videoLemelin.mp4`, autoplay/muette/loop, sans contrôles) façon pointlaz, en style OPUS. Contenu ancré en bas à gauche, texte blanc sur dégradé de lisibilité : titre **immense** « Les Entreprises / Christian Lemelin » (Onest) sur **deux lignes décalées** façon OPUS (1re ligne tirée vers la gauche `md:-ml-9` — déborde un peu du padding ; 2e ligne décalée à droite `md:ml-16` ; lignes **espacées** `leading-[1.02]` + `mt-2` ; `md:whitespace-nowrap` ; empile naturellement sur mobile). **Wrapper pleine largeur, ancré à gauche** (pas de `mx-auto`/`max-w`) : le titre colle au **bord gauche de la vidéo**. En dessous, une rangée `md:grid-cols-[1fr_auto] justify` — l'**intro est alignée sous « Christian Lemelin »** (`md:ml-16`, même indent que la 2e ligne) et le **lien `↗` est poussé au bord droit** de la vidéo. Le texte **apparaît après 3 s** (révélation en cascade). `data-header-theme="dark"`. Voir [`Hero.tsx`](src/components/sections/Hero.tsx).
- **Sections** : pas d'eyebrow ni de pilule — chaque section **s'ouvre directement sur son titre**.
- **Sections alternées** : blanc `#fff` / crème `#f3f3f1` / **noir neutre `#111111`** (token `ink`, texte blanc — **pas de teinte verte**). Très grand padding vertical (respiration).
- **Titres** : Onest, **casse normale**, taille géante (`clamp`), `leading` serré. Jamais tout en majuscules.
- **Stats** ([`StatsBar.tsx`](src/components/sections/StatsBar.tsx)) : très grands chiffres en **`text-foreground` neutre** (pas de bleu) + petit label gris, **centrés**. Section **fond crème (`bg-background`, sans bordure)** pour **fusionner avec le reste** (plus de bande blanche). Les chiffres sont **animés en compteur** (count-up de 0 → cible) au scroll dans la vue (`useInView` + `animate` sur un `useMotionValue`, formatage FR « 2 400 », suffixes `+`/`%` préservés, `tabular-nums`, fallback `prefers-reduced-motion`). Délimité par des **hairlines qui se dessinent** (`scaleX` 0→1, même easing `[0.76,0,0.24,1]` que le menu). En bas, un **mur de logos clients** : **deux rangées de tuiles** (`Marquee` primitive, sens opposés, `pauseOnHover`) qui défilent en boucle, **pleine largeur** (full-bleed `w-screen`, bords fondus vers le crème). Chaque tuile affiche un **vrai logo** (fichiers dans [`public/logos/`](public/logos/) — entreprises québécoises réelles servant de placeholder, à remplacer par les vrais clients), **en niveaux de gris atténués au repos → pleine couleur + léger soulèvement au survol** (`group-hover:grayscale-0` + `-translate-y`), de sorte que les autres tuiles **restent visibles** (aucune inversion, aucun bleu). Logos en `<img>` (PNG/SVG). La grille de chiffres est volontairement **compacte** (la vedette est le mur de logos).
- **Liste de services (Savoir-faire)** : **« Apple feature block »** reproduit de skiper-ui *Skiper76* — **bloc arrondi inséré** (`rounded-[2.5rem] overflow-hidden`, `min-h-[100svh]`) sur fond de page **crème** : la section externe garde une **fine marge** (`p-3 md:p-4`) pour laisser **voir le fond clair autour** des coins arrondis. `data-header-theme="dark"` sur la section externe (évite le scintillement du header sur la marge crème). **L'image englobe tout le bloc** (full-bleed `absolute inset-0`, `object-cover`, clippée par les coins arrondis) et fait un **crossfade façon Apple** au changement de service. Par-dessus, à gauche : le titre + des **pastilles-accordéon en verre dépoli** (`backdrop-blur`, `rounded-[1.6rem]`, icône `+` circulée ; la pastille **active** = verre sombre `bg-black/45` qui se **déplie** pour révéler description + lien `↗`). À droite, des **flèches haut/bas** (`CaretUp`/`CaretDown`, cercles en verre) **naviguent le carousel** de façon cyclique (centrées verticalement en desktop, en haut à droite sur mobile). Image **monochrome (`?grayscale`) fortement assombrie** (voiles `bg-black/45` + dégradés `from-black` à gauche/bas) → la section lit **noir minimaliste**, la photo n'est qu'un fond texturé subtil (placeholder ; remplacer par de vraies photos d'atelier monochromes). Empile sur mobile (contenu sur l'image). Voir [`SavoirFaire.tsx`](src/components/sections/SavoirFaire.tsx).
  - **Transition image (mesurée 1:1 sur le template via MCP Playwright)** : l'entrante surgit **de la droite** (`x:+8%`, `scale 0.92`, `opacity 0`) et **spring** vers l'état de repos (`scale 1.14` d'overscan, `x:0`) après un **décalage ~0,16 s** ; la sortante **glisse vers la gauche** (`x:-8%`, `scale 0.92`) et s'efface aussitôt → le « filmstrip » glisse vers la gauche, léger overshoot spring, très fluide. `AnimatePresence mode="sync"` + `motion/react`. Fallback `prefers-reduced-motion` = simple fondu.
- **Cartes process** (section sombre) : `rounded-2xl`, bordure subtile, titre ; la carte active est plus claire.
- **Grille projets** : **masonry asymétrique**, cartes = image `rounded-2xl` contenue + titre dessous.
- **Tarifs / cartes** : `rounded-2xl`, bordure `border-border`, fond crème ; prix géant, bouton (noir/bleu plein = primaire, blanc + bordure = secondaire), liste à puces avec flèches `↗` bleues, divider pointillé.
- **Boutons / liens** : pills arrondies ; **primaire** noir ou bleu plein (texte blanc), **secondaire** blanc + `border-border` ; les liens portent une flèche **`↗` bleue**.
- **Logo strip** clients en gris clair.
- **Dividers** : hairline `border-border` entre blocs ; pointillés à l'intérieur des cartes.
- **Page « À propos »** ([`APropos.tsx`](src/components/sections/APropos.tsx)) : mise en page éditoriale **deux colonnes décalées** (la colonne droite descend, `lg:mt-32`), chacune avec une **grille de 3 images** (2 sous-colonnes, ratios variés, `rounded-2xl` + hairline + parallax) et un bloc titre/texte. Reprend le block shadcnblocks « about6 », adapté OPUS. Empile sur mobile (colonne gauche d'abord).

## 7bis. Pages internes & primitives partagées *(ajout du chantier Solutions/Fabrication)*

**Contenu centralisé** — toute la copie marketing des pages internes vit dans [`src/content/`](src/content/) (TS typé, importé surtout par des server components → 0 coût bundle) : `types.ts`, `site.ts` (coordonnées **uniques** — adresse/tel/email), `image.ts` (helper `picsum`/`imageUrl`), `materials.ts` (**source unique** des 5 matériaux, consommée par le showcase `/fabrication` **et** le home `Materiaux.tsx` — **plus de pages de détail `/materiaux/[slug]`**), `solutions.ts`, `fabrication.ts`, `installations.ts`. **Plus de pages de détail `[slug]`** (ni matériaux ni solutions) : la copie solutions est **consolidée dans `/solutions`** (panneau « onglet » de `SolutionsIndex`, cf. §patterns) et les matériaux dans le modal du home. Drapeau `hasSourceCopy:false` = copie synthétisée (sans PDF source) à relire.

**Primitives réutilisables ajoutées** (`src/components/ui/`, `src/hooks/`) :
- [`DrawLine`](src/components/ui/DrawLine.tsx) — hairline qui se dessine (`scaleX`/`scaleY` 0→1, easing `[0.76,0,0.24,1]`), promu depuis `StatsBar`.
- [`RevealRow`](src/components/ui/RevealRow.tsx) — ligne à hairline + contenu en fade-up au scroll (utilisé par `ReasonsReveal`).
- [`useMediaQuery`](src/hooks/useMediaQuery.ts) — hook SSR-safe, extrait du `matchMedia` de `Solutions.tsx`.
- [`ParallaxImage`](src/components/ui/ParallaxImage.tsx) — prop `unoptimized` ajoutée (sources non-whitelistées, ex. data: URLs admin).
- [`ContactCTA`](src/components/sections/ContactCTA.tsx) — généralisé (props `heading/body/label/href` optionnelles, défauts = formulation d'origine).

**Composants de détail** — *supprimés* (`src/components/detail/` n'existe plus). Les anciens `DetailHero`/`ContentBlocks`/`GalleryStrip`/`ExploreMore`/`DetailLayout` servaient les pages `[slug]` (matériaux + solutions), toutes retirées. Le contenu utile vit désormais sur les pages d'aperçu (`/solutions`, `/fabrication`) et dans les modals.

**Patterns « wow » des pages internes :**
- **`SolutionsTimeline`** ([`/solutions`](src/components/sections/SolutionsTimeline.tsx)) — **« stroke-follows-scroll » façon skiper-ui [skiper19](https://skiper-ui.com/v1/skiper19)** (le code skiper19 est la **source de vérité**). Une grande **ligne bleue** (le **tracé EXACT de skiper19** : nœud bouclé + longues ondulations ; `strokeWidth 20`) **se dessine au scroll**, posée en grand **à droite** (motif). La scène est **sticky plein écran** ; les **6 sections** (solutions) s'affichent **en fondu enchaîné EN FONCTION du scroll** (à gauche : « 0X / 06 » mono + titre + tagline + carte image + lien « Découvrir »). **Clic → modale** (image + intro + points clés du 1er bloc `list`/`feature` + lien contact). **Pas de personnage** (idée de bonhomme allumette abandonnée — inutile). **Le scrubbing (dessin de la ligne via `strokeDashoffset`, opacité/translation des sections) est appliqué IMPÉRATIVEMENT au DOM via refs dans `place()`** appelé par `useMotionValueEvent(scrollYProgress,…)` — **aucun composant `motion` lié à une MotionValue de scroll** : **framer-motion v12 lève sinon une erreur WAAPI** « offsets must be monotonically non-decreasing » au montage (`pathLength`/`useTransform` de scroll en `style`). Ça évite aussi tout re-render. ⚠️ **Le `<section>` sticky ne doit pas avoir `overflow-hidden`** (casse `position:sticky`) — c'est la scène interne `h-screen` qui clippe. Seule la section visible a `pointer-events:auto` (les sections empilées en `absolute inset-0` intercepteraient sinon les clics). **Mobile** : même principe, ligne en haut, contenu dessous. **reduced-motion** : repli en **liste simple** (`SimpleList`). Deep-link `?s=<slug>` (`history.replaceState`, init `useSearchParams` → `<Suspense>`) conservé : **footer** et **`SavoirFaire`** y pointent.
- **`MaterialSwitcher`** ([`/fabrication`](src/components/sections/MaterialSwitcher.tsx)) — **spotlight matériau** plein cadre : crossfade « Apple » réutilisé de `SavoirFaire` (image full-bleed monochrome), sélection discrète (liste sans numéro + flèches + autoplay 6 s, pause au survol/hors-vue/reduced-motion). **Distinct** du home `Materiaux.tsx` (scroll horizontal GSAP) — ici stationnaire/discret.
- **`ReasonsReveal`** ([`/fabrication`](src/components/sections/ReasonsReveal.tsx)) — « 5 raisons » **sans numéro** : titre collant à gauche, lignes éditoriales (`RevealRow`) qui se révèlent au scroll à droite.
- **`JobAccordion`** ([`/emplois`](src/components/emplois/JobAccordion.tsx)) — offres en **accordéon typographique** sur hairlines (pattern `SectorTabs`), métadonnées en texte simple (pas de pilule), `ApplyModal` réutilisé.
- **`RealisationsGallery`** ([`/realisations`](src/components/realisations/RealisationsGallery.tsx)) — **projet vedette plein cadre** (parallax) + masonry pour le reste ; robuste à N dynamique (contexte admin). **Vedette interactive** : l'ordre suit la **position** définie en admin (la 1re est vedette par défaut) ; on peut arriver via `?featured=<id>` (clic d'une carte sur l'accueil) et **cliquer une carte du masonry pour la passer en grand** (`onSelect` → `setFeaturedId` + scroll vers le haut, re-anime la vedette via `key`). **La vedette est un carrousel** : si le projet a plusieurs photos, flèches `CaretLeft`/`CaretRight` (cercles en verre, `backdrop-blur`) + pastilles d'index, fondu enchaîné entre photos (`AnimatePresence`, respecte `prefers-reduced-motion`), index remis à 0 au changement de vedette. Le param est lu avec `useSearchParams` → la galerie est enveloppée dans `<Suspense>` côté page. Les cartes `RealisationCard` acceptent `href` (lien) **ou** `onSelect` (sélection en place) ; carrousel d'images accéléré (1,8 s).
- **`Installations`** ([`/installations`](src/components/sections/Installations.tsx)) — compteurs animés (façon `StatsBar`), bloc « feature » sombre, parallax.

**Footer** ([`Footer.tsx`](src/components/layout/Footer.tsx)) — refondu façon brochure : fond **sombre** (`bg-ink`), bandeau « Durabilité & innovation », bloc contact (coordonnées de `site.ts`), 3 colonnes (Entreprise / Médias / Nos emplois) dérivées du contenu, icônes sociales (Facebook, Instagram).

## 8. Pattern « Carousel + GLB » *(à venir — documenté, pas encore construit)*

Objectif : réutiliser le **carousel des matières** pour présenter les matériaux (inox, acier, aluminium, laiton, cuivre) et y intégrer des **objets GLB interactifs**.

Briques déjà existantes :
- [`src/components/sections/Materiaux.tsx`](src/components/sections/Materiaux.tsx) — carousel scroll horizontal piloté GSAP (desktop) + **carrousel horizontal à scroll-snap natif, swipeable** (mobile : `overflow-x-auto snap-x snap-mandatory`, tuiles `w-[78vw]` qui dépassent pour signaler le swipe, `touch-pan-x` pour laisser passer le scroll vertical de la page). **Chaque carte est cliquable** et se déplie en `linear-modal` : photo **en couleur** + code/nom en surimpression + affordance `+`. Le modal **réutilise la copie de `materials.ts`** (`mat.hero.intro` + le bloc `list` « pourquoi choisir » + `properties`) — **aucune duplication de contenu**, et **plus de lien vers une page de détail** (les pages `/materiaux/[slug]` ont été supprimées). Fond du modal = `ShaderBackdrop` monochrome **~90 % opaque** (la page reste faiblement visible derrière) qui **éclot depuis le centre de l'écran** (révélation `clip-path: circle()` 0→150 % au centre) ; le voile noir intégré du `linear-modal` est désactivé (`dim={false}`) pour que le shader soit le seul fond. (Photos placeholder à remplacer.)
- [`src/components/ui/SolutionsModel.tsx`](src/components/ui/SolutionsModel.tsx) — rendu GLB (react-three-fiber + `@react-three/drei`), seule visionneuse 3D vivante. Pour la rendre **interactive**, ajouter `OrbitControls`.

Plan d'intégration (futur) : chaque panneau de matière affiche une photo OU un modèle GLB de l'objet, manipulable à la souris/tactile, dans le respect des principes ci-dessus (fond neutre, accent bleu pour les détails, beaucoup d'espace).

---

## 9. Onboarding & smooth scroll

- **Preloader d'arrivée** ([`Preloader.tsx`](src/components/ui/Preloader.tsx)) — réplique du comportement de skiper-ui « Skiper8 » : overlay plein écran (ink) qui fait défiler des mots présentant l'entreprise (matières → nom), puis se retire avec une **animation de courbe SVG** (path animé) révélant le site. framer-motion + `AnimatePresence`. **Joue à chaque chargement complet** (se rejoue au refresh — voulu). **Pastille blanche** (plus de point bleu) et **noms en très grand** (`clamp(2.75rem,10vw,6.5rem)`). Liste de mots éditable en haut du fichier.
- **Smooth scroll Lenis** ([`LenisProvider.tsx`](src/components/providers/LenisProvider.tsx)) — enveloppe l'app, piloté par le ticker GSAP et synchronisé avec `ScrollTrigger` ; rend le scroll et toutes les animations au scroll (parallax, pin Matériaux) fluides. Expose `useLenis()` (utilisé par le Preloader pour le scroll-lock). CSS Lenis dans `globals.css`. `lerp: 0.1`.
### Scrollbar custom (look Firefox, partout)

On veut un scrollbar **fin façon Firefox, identique sur tous les navigateurs**. Le scrollbar **natif** ne permet pas ça de façon fiable, d'où la solution **DOM custom**.

**⚠️ Pièges du scrollbar natif (à ne pas refaire) :**
- Dès qu'on pose `scrollbar-width` (standard) sur un élément, **Chrome ignore complètement `::-webkit-scrollbar`** → impossible de combiner les deux pour styler.
- `html::-webkit-scrollbar` ne masque **pas** fiablement la barre racine de Chrome → il faut le sélecteur **`::-webkit-scrollbar` non préfixé (global)**.
- Le rendu natif varie selon la version de Chrome / l'OS → jamais cohérent.

**Solution retenue** (dans [`globals.css`](src/app/globals.css)) — masquer le natif :
```css
html, body { scrollbar-width: none; -ms-overflow-style: none; } /* Firefox / vieux Edge */
::-webkit-scrollbar { display: none; width: 0; height: 0; }     /* Chrome / Edge / Safari */
```
…puis le remplacer par un **élément DOM** : [`CustomScrollbar.tsx`](src/components/ui/CustomScrollbar.tsx).
- Barre fixe à droite, pouce **3px**, gris neutre `rgba(130,130,130,.5)` (lisible sur fond clair **et** sombre), arrondi.
- **Piloté par Lenis** (`useLenis()` → `scroll`/`limit`) avec fallback `window.scroll` ; recalculé sur `resize` + `ResizeObserver(body)`.
- **S'épaissit à ~7px + plus opaque au survol** (`group-hover`). `z-[90]`, desktop uniquement (`md:block`), masqué s'il n'y a pas de débordement.
- **Interactif** (comme un vrai scrollbar) : le pouce est **glissable** (`pointerdown`/`move`/`up` + `setPointerCapture`, mappe la position curseur → `lenis.scrollTo(y,{immediate,force})`) et la piste est **cliquable** (jump : centre le pouce sur le point cliqué). `touch-none select-none`, `cursor-grab`/`grabbing`.
- Comme c'est notre propre DOM, il s'affiche **à l'identique partout** — aucune dépendance aux quirks navigateur.

## Journal des modifications

| Date | Changement |
|---|---|
| 2026-06-28 | **Signature studio dans la console (easter egg façon Lando Norris).** Au montage, [`ConsoleSignature`](src/components/ui/ConsoleSignature.tsx) (rendue par [`layout.tsx`](src/app/layout.tsx)) imprime dans la **console développeur** le wordmark **« GROUPE NOLET·ANDREWS »** comme **image de fond** d'un `console.log` (technique `background-image` + `padding`/`font-size`, comme le site de Lando Norris), suivi d'une **légende** « Conception & développement web · Groupe Nolet Andrews · noletandrews.ca » dans l'**orange du logo `#FDB800`**. Le logo source (récupéré du site GNA, fond blanc) a été **détouré en fond transparent** (flood-fill depuis les bords, intérieurs de lettres + point orange conservés) et sauvegardé en [`public/assets/logo-gna.png`](public/assets/logo-gna.png) (PNG transparent, 750×145) → le wordmark blanc ressort sur le gris sombre de la console. Affichage **unique par session** (garde `window.__gnaSignaturePrinted`, anti double-montage strict). L'image de fond console n'est rendue que par Chromium/Edge ; la **légende texte** est le repli universel (Firefox/Safari). Aucun rendu visible dans la page. |
| 2026-06-28 | **`/solutions` refait façon skiper-ui « skiper19 ».** L'ancien `SolutionsIndex` (index typographique survol/onglet) est **remplacé** par [`SolutionsTimeline`](src/components/sections/SolutionsTimeline.tsx) : une **ligne bleue** (tracé **exact de skiper19**, source de vérité) qui **se dessine au scroll** (grande, à droite), et les **6 sections** qui **s'affichent en fondu en fonction du scroll** (à gauche : numéro + titre + carte image + lien) ; **clic → modale**. **Pas de personnage** (l'idée du bonhomme allumette a été abandonnée). **Scrubbing 100 % impératif** (refs + `place()` via `useMotionValueEvent`) — **aucun `motion` lié à une MotionValue de scroll**, sinon **framer-motion v12 lève une erreur WAAPI** « offsets must be monotonically non-decreasing » au montage. ⚠️ Le `<section>` sticky **sans** `overflow-hidden` (casse `position:sticky`) ; seule la section visible a `pointer-events:auto`. **Mobile** : ligne en haut, contenu dessous. **reduced-motion** : liste simple. Composant `SlidingFigure` supprimé. Voir §7bis. |
| 2026-06-28 | **Page de maintenance activable depuis Vercel.** Nouvelle page autonome [`/maintenance`](src/app/maintenance/page.tsx) (style OPUS : fond crème, label mono, titre en casse normale, hairline, coordonnées de `site.ts` ; `robots: noindex`), **rendue sans chrome** (`SiteChrome` la traite comme `/admin`). Activation **sans code** via la variable d'env **`MAINTENANCE_MODE=true`** dans Vercel (puis redéploiement) : [`proxy.ts`](src/proxy.ts) réécrit tout le site public vers `/maintenance` en **503**, tout en laissant `/admin`, l'API et les internes Next joignables (matcher élargi à toutes les routes ; la garde du dashboard admin est conservée). Voir README « Maintenance mode ». |
| 2026-06-28 | **Intro jouée une seule fois par session + fond du modal matériaux.** (1) **Preloader** : se **rejoue à chaque chargement** (voulu — le test « une fois par session » a été annulé sur demande) ; **pastille blanche** (plus de point bleu) + **noms agrandis** (`clamp(2.75rem,10vw,6.5rem)`). (2) Fond du **modal matériaux** : `ShaderBackdrop` passé à **~90 % opaque** (page faiblement visible) et **éclot depuis le centre de l'écran** (révélation `clip-path: circle()` 0→150 %) ; voile noir intégré du `linear-modal` désactivé via la nouvelle prop `dim={false}`. |
| 2026-06-28 | **CTA contact : grande section sur l'accueil, pop-out ailleurs.** La section `ContactCTA` (« Un projet métal en tête ? ») n'apparaît plus que sur `/` ; **retirée des 6 pages internes** (réalisations, solutions, emplois, fabrication, installations, à-propos — imports/vars `cta` nettoyés). Partout ailleurs (sauf `/contact`), un **bouton « Contactez-nous »** flottant en bas à droite — **noir, non masquable, qui déplie au clic un panneau avec le message + un lien** (nouveau [`ContactFab`](src/components/ui/ContactFab.tsx), rendu par [`SiteChrome`](src/components/layout/SiteChrome.tsx)) — mène au formulaire `/contact`. Aussi : modal matériaux scrollable sur mobile (`data-lenis-prevent`) et **overlay menu responsive** (scroll + espacements réduits, partenaires masqués sur mobile → « Nous joindre » ne déborde plus). |
| 2026-06-28 | **Accueil — mur de logos clients + chiffres compacts.** « Ils nous font confiance » devient un **mur de logos** : vrais logos d'entreprises québécoises (Pomerleau, EBC, Cascades, Quirion Métal, Concordia, Saputo, Agropur, Olymel, Lassonde — placeholders dans [`public/logos/`](public/logos/), à remplacer par les vrais clients), **pleine largeur**, **niveaux de gris au repos → couleur + léger soulèvement au survol** (les autres tuiles restent visibles — remplace l'ancien survol qui inversait la carte en noir et « effaçait » les voisines). `StatsBar` (chiffres) rendu **plus compact** pour donner la vedette au mur. Logos sélectionnés/vérifiés via un workflow (entreprises QC réelles + domaines canoniques) puis téléchargés (Wikimedia / sites officiels). |
| 2026-06-28 | **Scrollbar custom rendu interactif.** Le pouce de `CustomScrollbar` (qui n'était que décoratif, `aria-hidden`, donc impossible à attraper puisque le natif est masqué) devient **glissable** (drag → `lenis.scrollTo` immédiat) et la **piste cliquable** (jump). Vérifié : drag de 350px → scroll 0→1163, clic bas de piste → scroll au max. |
| 2026-06-28 | **Navbar + footer — nouveaux logos (SVG noir/blanc) + sync Storage.** Le logo unique PNG + filtre `invert` est remplacé par **deux SVG à couleur cuite** : noir (sur fond clair) et blanc (sur fond sombre / menu ouvert), qui **se fondent en opacité** selon le thème — plus de `invert`. Le **footer** (fond sombre) affiche le **wordmark blanc** sous les colonnes de liens (au-dessus du bas de page). Ajoutés à `SITE_MEDIA` (`logoNoir`/`logoBlanc`) et au pipeline `media:sync`. ⚠️ Les SVG sources étaient un wordmark 600×92 noyé dans un canvas carré 1500×1500 (≈85 % de padding) → **`viewBox` recadré** à la bande du wordmark (`0 630 1500 240`) sinon le logo rendait minuscule ; clés versionnées (`logo-ecl-noir-v2.svg`/`-blanc-v2.svg`) pour casser le cache navigateur. `next/image` en `unoptimized` (ratio 1500×240). |
| 2026-06-28 | **Hero — titre réaligné + apparition « symboles métalliques ».** (1) **Alignement** : le titre ne déborde plus du bord gauche — suppression des décalages `-ml`/`ml-16` ; le titre et le paragraphe/lien partagent désormais **le même bord gauche** (padding du conteneur). (2) **Apparition** : le titre se **résout à partir des symboles d'éléments métalliques** (nouvelle primitive [`RandomizedTextEffect`](src/components/ui/text-randomized.tsx)) déclenchée après le délai du Hero (`start={show}`), au lieu du simple fondu/translate. Glyphes simples → pas de reflow ; reduced-motion = texte direct. |
| 2026-06-28 | **Modal matériaux — refonte « draft 3D » narrative.** Le modal monotone (image bandeau + corps empilé) devient une **fiche éditoriale en deux panneaux** (desktop `md:flex-row`, mobile empilé) qui **s'incline en perspective** vers le curseur : le panneau entier est un objet physique (`rotateX`/`rotateY` lissés par `useSpring`, `[perspective:1500px]` sur le parent), avec un **reflet métallique** (`radial-gradient` + `mix-blend-soft-light`) qui suit la souris sur l'image, et le **titre en parallax** (translate inverse du tilt). Panneau gauche = la matière (image plein cadre, code/nom/sous-titre) ; panneau droit = **l'histoire** : intro + raisons présentées en **chapitres numérotés** (`01/02/03`, ligne de séparation) + chips de propriétés. Tout en transform/opacity (GPU) ; **désactivé sous `prefers-reduced-motion`** (rendu statique). `DialogContent.style` élargi à `MotionStyle` pour accepter les `MotionValue` de transform. |
| 2026-06-28 | **Modal matériaux — fond simple + « + » lisible sur photos pâles.** (1) Suppression du `ShaderBackdrop` derrière le modal : la `DialogContainer` repasse au voile sombre intégré par défaut (`dim` non passé) — plus de fond animé. (2) La pastille « + » des cartes matériaux passe d'un verre blanc translucide (`bg-white/15`, invisible sur les photos claires) à un **verre sombre** (`bg-black/35` + `ring-white/25` + `shadow-sm`, survol `bg-black/55`) : le « + » blanc garde son contraste quelle que soit l'image. |
| 2026-06-28 | **Logos clients servis depuis Supabase Storage.** Les logos du marquee « Ils nous font confiance » (`StatsBar`) ne sont plus des fichiers `public/logos/*` codés en dur : ils passent par le pipeline média existant — clés dans `SITE_MEDIA.clients` ([`lib/media.ts`](src/lib/media.ts), dossier `photos/logo/clients/`), résolues via `mediaUrl()`, uploadées par `media:sync` (ajout du type `image/svg+xml`). Sources toujours dans `public/logos/`. |
| 2026-06-28 | **Solutions — onglet anti-saccade + footer dédoublonné.** (1) `SolutionsIndex` : on **n'anime plus aucune propriété qui reflow** (width/font-size/height) — c'était la cause des saccades, même avec `layout`. Le passage en mode compact (colonne étroite + noms réduits) est **instantané** (un seul reflow) ; seuls le **panneau** et les micro-interactions s'animent, et **uniquement via transforms GPU** (`opacity`/`translate`). Plus de `LayoutGroup`/`layout`. (2) Le **« tassement »** est **réservé au desktop** — sur mobile, accordéon simple (fondu + léger translate, **pas d'animation de hauteur**). (3) Changement d'onglet : boîte du panneau montée en continu, seul le **contenu se fond**. (4) Conteneur de la section élargi (`max-w-[1400px]` → `max-w-[1600px]`) + `text-balance` sur les noms géants → fini les mots orphelins en fin de ligne. (5) Footer : suppression du doublon « Devenir client » (même cible `/contact` que « Nous contacter »). |
| 2026-06-28 | **Solutions — pages de détail supprimées, tout consolidé sur `/solutions`.** Les routes `/solutions/[slug]` (+ `not-found`) et **tout `src/components/detail/`** (devenu mort) sont supprimées. `SolutionsIndex` garde ses **animations de survol** (aperçu suiveur de curseur, dim, glissement du nom) mais, au **clic**, n'ouvre plus une page : la liste **se tasse à gauche** (`md:w-[40%]`, noms réduits) et un **panneau « onglet »** affiche les **infos utiles** de la solution (aperçu + intro + points clés du 1er bloc `list`/`feature` + lien contact) — desktop = panneau latéral `md:sticky`, mobile = **accordéon inline**. Données dérivées côté serveur dans `page.tsx` (`buildItems`). Deep-link `?s=<slug>` (`history.replaceState`, init via `useSearchParams` → `<Suspense>`) : **footer** et **`SavoirFaire`** repointés vers `/solutions?s=…`. |
| 2026-06-28 | **Suppression des pages de détail des métaux.** Les routes `/materiaux/[slug]` (page + not-found) sont supprimées (la vue d'ensemble vit sur `/fabrication`, le détail dans le modal du home). Tous les liens entrants nettoyés pour éviter les 404 : **footer** (colonne « Expertises métaux » retirée → 4 colonnes), **modal `Materiaux`** (lien « Voir la page complète » retiré), **showcase `/fabrication`** (lien « Explorer ce matériau » retiré), **pages solutions** (tuiles « Les matériaux de cette solution » retirées). `/materiaux` (nu) redirige toujours vers `/fabrication`. |
| 2026-06-28 | **Modal matériaux — fondu/zoom au lieu du morph + fond plus noir.** Les cartes `Materiaux` passent en `morph={false}` (nouvelle prop de `Dialog`) : le modal **fond/zoome** proprement à l'ouverture au lieu de redimensionner l'image du trigger (qui « disparaissait en arrière » pour se redimensionner — déplacements parasites). Fond du modal plus dominant en **noir** : voile `bg-black/85 → bg-black/92` + `ShaderBackdrop` `opacity-55 → opacity-25`. |
| 2026-06-28 | **Accueil — page sans bleu, marquee clients, modal matériaux.** (1) **Aucun bleu sur `/`** : l'accent est neutralisé en **noir** via un `@theme` runtime + `body:has([data-home-neutral])` (cascade header/footer/sections, pages internes inchangées) ; sections sombres → accent **blanc** (voir §2). (2) **« Ils nous font confiance »** (`StatsBar`) : l'ancien ruban de noms devient un **marquee à deux rangées de cartes** clients/partenaires (nouvelle primitive [`Marquee`](src/components/ui/marquee.tsx) + `cn()` + keyframes dans `globals.css`, `prefers-reduced-motion` respecté), **pleine largeur**, **survol → carte noire**. (3) **Matériaux** : les cartes deviennent **cliquables** et se déplient en **`linear-modal`** (nouvelle primitive morphante portalisée) affichant la **copie réutilisée des pages de détail** (zéro duplication) ; **photos en couleur**, fond de modal **monochrome opaque** via [`ShaderBackdrop`](src/components/ui/ShaderBackdrop.tsx) (`@shadergradient/react`, lazy + error-boundary). Vérifié responsive 390/768/1440 via MCP Playwright. Logos clients = **placeholders** (vrais logos à fournir). |
| 2026-06-24 | **Nettoyage post-migration Vercel.** Suppression de code/assets morts : `ModelViewer.tsx` (visionneuse GLB jamais montée — le rendu GLB vivant est `SolutionsModel.tsx`) et `SolutionsHotspots.tsx` (jamais importé), le placeholder `public/models/test-model.glb`, les SVG de scaffold Next (`next/vercel/file/globe/window.svg`) et `public/videos/hero.mp4` inutilisés. Références à `ModelViewer` redirigées vers `SolutionsModel` ci-dessus. (Hors design : retrait de l'API NestJS `backend/`, de la stack Docker et de la suite E2E Playwright legacy.) |
| 2026-06-02 | Création du système de design aligné sur OPUS. Bascule des tokens : accent ambre `#f5a020` → bleu `#0048f9` ; foreground `#111113` → `#0c120c` ; surface → blanc `#ffffff` ; bordures → `rgba(151,151,151,.2)`. Polices : Bebas Neue + Barlow Condensed → **Onest** + **Fragment Mono**. Fichiers touchés : `globals.css`, `layout.tsx`. |
| 2026-06-02 | Ajout d'un MCP Playwright (`.mcp.json`) + captures de référence d'OPUS. Documentation du **système de layout OPUS** (section 7) suite à l'analyse visuelle : le layout actuel divergeait fortement (hero vidéo plein écran, cartes photo empilées, titres majuscules) → refonte des sections à venir. |
| 2026-06-02 | **Refonte layout « Full OPUS »** de toute la page d'accueil. Nouvelles primitives `Eyebrow` / `ArrowLink` / `Tag`. Hero type-led (titre géant + vidéo contenue, remplace le hero plein écran). Savoir-faire → accordéon numéroté 01–05 + visuel contenu. Matériaux → carousel de cartes contenues. Solutions & ContactCTA → sections sombres `#0c120c`. Réalisations → grille masonry + tag pills. StatsBar → grands chiffres bleus. Titres passés en **casse normale**. Bouton header : texte blanc sur bleu, pill, glow ambre retiré. |
| 2026-06-02 | **Raffinements demandés.** (1) Retrait de toute teinte verte → noirs neutres (`foreground #141414`, token `ink #111111`). (2) Plus aucun **texte bleu** (stats, codes matières → neutres) ; bleu réservé flèche/point/bouton. (3) Liens = texte souligné neutre + flèche bleue (`ArrowLink` revu). (4) Nouveau `ParallaxImage` appliqué aux cartes (Solutions, Réalisations, Matériaux). (5) **Nav refaite façon OPUS** : barre minimale repliée + overlay plein écran bleu (menu hamburger/« Menu »). |
| 2026-06-02 | **Preloader d'arrivée + smooth scroll.** Nouveau `Preloader` (réplique skiper8 : mots défilants présentant l'entreprise + reveal courbe). Installation de **Lenis** + `LenisProvider` (sync GSAP/ScrollTrigger) pour fluidifier scroll & animations. Parallax renforcé (amplitude 15%). Liens du menu **centrés**. Correctif avertissement next/image (parent `sticky` → wrapper `relative`). |
| 2026-06-02 | **Parallax plus marqué** (amplitude 19% / image 170%, ~32% de mouvement) — vérifié (translateY change au scroll). **UI globale custom** (jamais les défauts navigateur) : `::selection` bleu accent, liens `a { color: inherit; text-decoration: none }`. |
| 2026-06-02 | **Scrollbar custom (DOM) façon Firefox.** Le scrollbar natif s'avérait incohérent/non-appliqué selon la version de Chrome (conflit `scrollbar-width` ↔ `::-webkit-scrollbar`). Solution définitive : natif masqué + composant `CustomScrollbar` (élément DOM fin de 3px piloté par Lenis, identique partout, épaissit au survol). |
| 2026-06-02 | **Animation du menu (inspirée pointlaz.com).** Ouverture plus longue (~1,7 s) en **révélation graduelle ligne par ligne** (liens montant depuis un masque + traits se dessinant en cascade) et **animation de fermeture** qui rejoue l'inverse. Variants framer-motion dynamiques (`custom={i}`) dans `Header.tsx`. |
| 2026-06-03 | **Menu refondu façon pointlaz, en bleu OPUS.** Layout encadré : **cadre + lignes de séparation qui se dessinent** progressivement, **grands liens à gauche**, **infos/contact à droite**, **partenaires (placeholder) en bas**, bouton ✕ pour fermer, scroll-lock Lenis. Fond bleu accent (pas noir) pour respecter OPUS. |
| 2026-06-03 | **Hero = bannière vidéo plein cadre** (`videoLemelin.mp4`, loop/muet/autoplay) façon pointlaz, style OPUS ; texte (« Christian Lemelin » + intro + lien) révélé **après 3 s**. Remplace le hero à vidéo contenue. |
| 2026-06-03 | **Retrait des badges et de la numérotation.** Suppression des primitives `Eyebrow` (pill point bleu + label) et `Tag` (pill outline) et de tous leurs usages (Hero, SavoirFaire, Solutions, Réalisations, Matériaux, ContactCTA) → les sections s'ouvrent directement sur leur titre. Retrait de la numérotation de section (`01`–`05`) de l'accordéon Savoir-faire et de la liste d'intro Matériaux. Bleu accent désormais réservé à la flèche `↗` et au fond de bouton (plus de point d'eyebrow). |
| 2026-06-03 | **Page « À propos » (`/a-propos`).** Nouvelle page reprenant le layout du block shadcnblocks « about6 » : deux colonnes éditoriales décalées, grilles de 3 images (parallax `rounded-2xl`) + blocs de texte, responsive (empile sur mobile). Composants `APropos.tsx` + `app/a-propos/page.tsx`. La route `/a-propos` est celle déjà câblée dans le menu et le footer. |
| 2026-06-03 | **Savoir-faire — titre agrandi** (`clamp(2rem,5vw,3.75rem)` → `clamp(2.5rem,6vw,4.5rem)`). L'essai d'accordéon piloté par le scroll (pin/scrub) a été **annulé** : retour à l'ouverture au clic. |
| 2026-06-03 | **Savoir-faire → « Apple feature block » (skiper-ui Skiper76).** Refonte de la section en **plein écran noir** (`min-h-[100svh]`, **container retiré**, `data-header-theme="dark"`). **L'image englobe toute la section** (full-bleed) et fait un **crossfade façon Apple** reproduit à l'identique (mesuré via MCP Playwright : entrée depuis la droite `x:+8%`/`scale 0.92` → repos `scale 1.14` après décalage 0,16 s, **sortie glissée vers la gauche**, spring + overshoot). Par-dessus : titre + **pastilles-accordéon en verre dépoli** (`backdrop-blur`, icône `+`, dépliage description + flèche `↗`). Image **monochrome (`?grayscale`) + voiles noirs** pour un rendu **noir minimaliste** (placeholder). `motion/react` `AnimatePresence`, responsive (empile sur mobile), fallback `prefers-reduced-motion`. |
| 2026-06-03 | **StatsBar refondu + header auto-hide.** (1) **StatsBar** : fond passé en **crème (`bg-background`, bordure retirée)** pour fusionner avec le reste, chiffres **centrés** et **animés en compteur** (count-up au scroll, formatage FR, fallback reduced-motion), plus d'**espace**, **hairlines qui se dessinent** (façon menu) pour délimiter, et **marquee de partenaires** défilant **droite→gauche** en boucle (bords fondus). (2) **Header** : **auto-hide** — se cache au scroll vers le bas, réapparaît animé au scroll vers le haut (`motion.header`). |
| 2026-06-03 | **Savoir-faire — cadre arrondi + flèches de navigation.** Le bloc devient un **bloc arrondi inséré** (`rounded-[2.5rem]`, marge `p-3 md:p-4`) qui **laisse voir le fond crème** autour des coins. Ajout de **flèches haut/bas** (`CaretUp`/`CaretDown`, cercles en verre dépoli) pour **parcourir le carousel** de façon cyclique (centrées à droite en desktop, en haut à droite sur mobile) ; chaque navigation rejoue le crossfade Apple. |
| 2026-06-05 | **Hero — titre OPUS sur deux lignes décalées.** « Les Entreprises » / « Christian Lemelin » désormais en **deux `<span>` block** décalés en diagonale façon OPUS (1re ligne `-ml-3`, 2e ligne `ml-[0.55em]`, `md:whitespace-nowrap`) et **agrandi** (`clamp(3rem,11vw,9.5rem)` → `clamp(3rem,11vw,10rem)`). Puis, sur demande : **lignes plus séparées** (`leading-[0.88]` → `leading-[1.02]` + `mt-2`), **wrapper pleine largeur ancré à gauche** (retrait `mx-auto`/`max-w-[1400px]`) → le titre colle au **bord gauche de la vidéo**, **intro alignée sous « Christian Lemelin »** (`md:ml-16`, même indent que la 2e ligne `md:ml-16`, 1re ligne `md:-ml-9`) et **lien `↗` poussé au bord droit**. Empile sans rognage sur mobile (nowrap desktop only). Alignements vérifiés au pixel via MCP Playwright (1920 : 2e ligne et intro à 112 px, lien à droite 1872 px). |
| 2026-06-21 | **Courriels de notification (Resend) aux couleurs du site.** Les formulaires « Nous joindre » et candidature envoient désormais un courriel HTML à l'entreprise via Resend ([`src/lib/server/mail/email-templates.ts`](src/lib/server/mail/email-templates.ts)). Le gabarit reprend l'esthétique OPUS en **HTML compatible courriel** (layout en tables + styles inline, polices système en repli d'Onest/Fragment Mono) : fond crème `#f3f3f1`, carte blanche à **hairline** + filet d'accent bleu en tête, **labels mono** majuscules `tracking` large en gris, titre en **casse normale**, panneau message crème, et **un seul accent bleu** réservé au bouton pill « Répondre » (texte blanc) — conforme à la règle « pas de texte bleu ». Valeurs hex pleines (et non `rgba`) car les clients courriel gèrent mal la transparence. Voir README « Email notifications (Resend) ». |
| 2026-06-19 | **Réalisations — ordre admin + vedette interactive + carrousel accéléré.** (1) Nouveau champ `position` (backend Prisma + migration) → les réalisations ont un **ordre défini en admin** (flèches « déplacer avant / après » `ArrowLeft`/`ArrowRight` sur chaque carte du tableau de bord ; persiste via `PATCH /realisations/reorder`). L'ordre est respecté partout (accueil épinglées, page `/realisations`, admin). (2) `/realisations` : la **vedette est cliquable/échangeable** — depuis l'accueil, cliquer une carte ouvre `/realisations?featured=<id>` (ce projet en grand) ; sur la page, cliquer une autre carte la passe en vedette (`onSelect`, re-anime via `key`, scroll vers le haut). `useSearchParams` → galerie sous `<Suspense>`. (3) `RealisationCard` : props `href`/`onSelect`, **carrousel accéléré 3 s → 1,8 s**. (4) Réordonnancement **animé** au tableau de bord : les cartes **glissent** vers leur nouvelle position (spring `layout` de motion/react) au lieu de sauter. |
| 2026-06-21 | **Réalisations — la vedette devient un carrousel de photos.** Sur `/realisations`, le **projet vedette plein cadre** affiche désormais **toutes ses photos** : si le projet en a plusieurs, flèches `CaretLeft`/`CaretRight` (cercles en verre `bg-black/35` + `backdrop-blur`, centrées sur les côtés) + **pastilles d'index** (coin haut-droit), **fondu enchaîné** entre photos (`AnimatePresence`, respecte `prefers-reduced-motion`). L'index repart à 0 quand on change de vedette (clic sur une autre carte). Les projets à une seule photo n'affichent aucun contrôle. La **mise en page masonry** (accueil + `/realisations`) reste inchangée (CSS multi-colonnes d'origine). |
| 2026-06-21 | **Réalisations admin — réordonnancement par glisser-déposer.** Les flèches « déplacer avant / après » (`ArrowLeft`/`ArrowRight`) du tableau de bord sont remplacées par un **glisser-déposer**, en **conservant la grille de cartes** (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`) — pensée pour grossir au fil des projets. Implémenté avec **`@dnd-kit`** (`core` + `sortable` + `utilities` ; `DndContext` + `SortableContext` en `rectSortingStrategy`, adapté aux grilles multi-colonnes, ce que `Reorder` de motion ne gère pas). Chaque carte garde son design (image `rounded-2xl`, badges Accueil / nb d'images, titre, actions) ; l'unique nouveauté est une **poignée de glissement** (`DotsSixVertical`, `cursor-grab`/`touch-none`) à gauche de la rangée d'actions, là où étaient les flèches — le drag est lié à la poignée (`{...listeners}`) pour garder épingler / modifier / supprimer cliquables. Carte active surélevée (`shadow-xl`, hairline `border-accent/40`, `z-20`). Sensors `PointerSensor` (distance 6 px) + `KeyboardSensor` (réordonnancement au clavier). Ordre **optimiste** (maj locale immédiate via `arrayMove`, persiste via `PATCH /realisations/reorder`, revert si l'API échoue) — `moveRealisation` (échange par pas) remplacé par `reorderRealisations(orderedIds)` dans `admin-context`. Label « Glissez-déposez pour réordonner ». Au passage, correction d'une coquille du formulaire d'emploi (« Créerl'emploi » / « Mettre à jourl'emploi » → espace rétabli). |
| 2026-06-04 | **Chantier Solutions / Fabrication / détails (18 routes).** Création des pages `/solutions` (index typographique au survol `SolutionsIndex`), `/fabrication` (`ReasonsReveal` sans numéro + `MaterialSwitcher` plein cadre), `/installations`, `/contact`, des templates `[slug]` (6 détails solutions + 5 matériaux, server components SSG) et refonte de `/realisations` (vedette + masonry) et `/emplois` (`JobAccordion`). **Contenu centralisé** dans `src/content/` (coordonnées **du PDF** : 680 rue du Carbone / (418) 841-1220 — remplace `418 682-1750` dans Header + Footer). Nouvelles primitives `DrawLine`/`RevealRow`/`useMediaQuery`, composants `src/components/detail/*`, `ContactCTA` & `ParallaxImage` généralisés. **Footer** refondu (sombre, 5 colonnes brochure, liens dérivés du contenu). `Materiaux.tsx` consomme désormais `MATERIALS` (source unique). Contraintes respectées : aucun texte bleu, aucun badge/numéro/eyebrow/carte ; voir §7bis. Pages sans copie PDF (`mobilier-laboratoire`, `prototypes`, `pieces-sur-mesure`, `aluminium`, `laiton`, `cuivre`) = copie synthétisée (`hasSourceCopy:false`) à relire. |
