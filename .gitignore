node_modules
.next
/tmp

# TypeScript incremental build cache
*.tsbuildinfo

# Local secret backups (e.g. .env.prod.bak)
*.bak

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# dotenv environment variable files (keep *.example committed)
.env
.env.development
.env.test
.env.production
.env.local
.env.prod
.env.docker

# temp directory
.temp
.tmp

# Playwright MCP test artifacts (screenshots, page snapshots, console logs)
.playwright-mcp/

# Sentry build-plugin auth token
.env.sentry-build-plugin

# Local Windows/WSL dev helper (not portable to non-WSL hosts)
/dev.ps1
