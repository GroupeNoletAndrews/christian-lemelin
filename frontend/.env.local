# ─────────────────────────────────────────────────────────────
# Frontend (Next.js) — LOCAL DEV env.  GITIGNORED.
# Auto-loaded by `next dev` / `next build` (Next reads .env.local).
# Only NEXT_PUBLIC_* are exposed to the browser; the rest stay server-side.
# ─────────────────────────────────────────────────────────────

# Public URL the browser uses to reach the backend API (baked into the build).
NEXT_PUBLIC_API_URL=http://localhost:3001

# Supabase (client + SSR) — read in src/utils/supabase/{client,server}.ts
NEXT_PUBLIC_SUPABASE_URL=https://lgfsefdzrpbwyzxumaoj.supabase.co/
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_lf7TMeAaAmq3YPVUEF3W4w_AiEX1ptH

# ─── Not yet used on this branch (kept for reference, commented) ───
# Server-side Supabase service-role key. No frontend code reads it yet; uncomment
# only if you add server actions / route handlers that need elevated DB access.
# Never send this to the browser (no NEXT_PUBLIC_ prefix — keep it that way).
# SUPABASE_SECRET_KEY=sb_secret_8C5y1WYxBe3G0xEVxeObBg_0GVh0Oum

# Sentry sourcemap-upload token (build-time). Belongs to the "baseline monitoring"
# branch (the src/instrumentation.ts work); unused on feat/backend until that lands.
# SENTRY_AUTH_TOKEN=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
